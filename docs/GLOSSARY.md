# MCP Tools - Glossary

**Глоссарий терминов и концепций MCP**

---

## A

### AST (Abstract Syntax Tree)
**Абстрактное синтаксическое дерево** - структурированное представление исходного кода в виде дерева узлов. MCP инструменты используют AST parsing для точного анализа кода вместо regex.

**Пример:**
```typescript
// Код:
import { Button } from './ui/button';

// AST представление:
{
  type: 'ImportDeclaration',
  specifiers: [{ type: 'ImportSpecifier', imported: 'Button' }],
  source: './ui/button'
}
```

**Преимущество:** AST игнорирует комментарии и строки, находит только реальные конструкции кода.

---

### Audit
**Аудит** - комплексная проверка кодовой базы по определенному критерию (безопасность, доступность, технический долг).

**Инструменты аудита:**
- `npm run mcp:security-audit` - безопасность
- `npm run mcp:a11y-audit` - доступность (accessibility)
- `npm run mcp:tech-debt` - технический долг

**Результат аудита:**
- Score (0-100)
- Grade (A-F)
- Issues (список проблем с приоритетами)

---

### Audit Log
**Лог аудитов** - JSON файл (`docs/audits/AUDIT_LOG.json`), содержащий историю всех запусков MCP инструментов с результатами и трендами.

**Структура:**
```json
{
  "tools": {
    "securityAudit": {
      "executionCount": 5,
      "lastExecuted": "2025-01-09T12:30:00Z",
      "results": [
        { "timestamp": "...", "score": "90/100", "grade": "A" }
      ]
    }
  }
}
```

**Команды:**
- `npm run mcp:audit-history -- securityAudit` - история инструмента
- `npm run mcp:audit-summary` - общая статистика

---

## C

### Cache
**Кэш** - временное хранилище результатов MCP инструментов для ускорения повторных запросов.

**Где хранится:** `docs/cache/MCP_CACHE.json`

**TTL (Time To Live):**
- Git-based: 5 минут
- Audits: 30 минут
- Analysis: 2 часа

**Команды:**
- `npm run mcp:cache-status` - статус кэша
- `npm run mcp:cache-clear` - очистка кэша

---

### Cache Hit Rate
**Процент попаданий в кэш** - метрика эффективности кэширования.

**Формула:**
```
Hit Rate = (Cache Hits / Total Requests) * 100%
```

**Интерпретация:**
- < 50% - плохо (кэш неэффективен)
- 50-70% - нормально
- > 70% - отлично

**Проверка:**
```bash
npm run mcp:cache-status | grep "Hit Rate"
# Hit Rate: 75% (отлично)
```

---

### Callers Analysis
**Анализ вызовов** - поиск кто/где вызывает определенную функцию (обратные ссылки).

**Команда:**
```bash
npm run mcp:callers -- handleSubmit
```

**Результат:**
```
src/pages/LoginPage.tsx:45 → calls handleSubmit()
src/components/Form.tsx:12 → calls handleSubmit()
```

---

### Code Health Score
**Оценка здоровья кода** - комплексная метрика (0-100) качества кодовой базы.

**Рассчитывается как:**
```
Health Score = (Security Score + A11y Score + (100 - Tech Debt Score)) / 3
```

**Grade mapping:**
- 90-100: A (отлично)
- 80-89: B (хорошо)
- 70-79: C (удовлетворительно)
- 60-69: D (плохо)
- < 60: F (провал)

**Команда:**
```bash
npm run mcp:code-health-check
```

---

### Composite Skills
**Композитные навыки** - MCP инструменты объединяющие несколько базовых инструментов в один.

**Три главных:**
1. `full-project-audit` = Security + A11y + Tech Debt
2. `code-health-check` = Complexity + Unused + Performance + State
3. `project-docs` = Components + API + i18n + Markdown generation

**Преимущество:** Параллельное выполнение (быстрее чем последовательно).

---

### Cyclomatic Complexity
**Цикломатическая сложность** - метрика сложности кода, основанная на количестве независимых путей выполнения.

**Формула:**
```
Complexity = E - N + 2P
E = edges (переходы)
N = nodes (узлы)
P = connected components (обычно 1)
```

**Интерпретация:**
- 1-5: Простая функция
- 6-10: Средняя сложность
- 11-20: Высокая сложность (нужен рефакторинг)
- 21+: Очень высокая (критично)

**Команда:**
```bash
npm run mcp:complexity
```

---

## D

### Dead Code
**Мертвый код** - код который никогда не выполняется или не используется.

**Типы:**
- Неиспользуемые экспорты (`export function unused()`)
- Недостижимый код (после `return`)
- Неиспользуемые переменные

**Поиск:**
```bash
npm run mcp:unused
```

---

### Design Tokens
**Дизайн токены** - переиспользуемые дизайн-переменные (цвета, шрифты, отступы).

**Пример:**
```css
:root {
  --color-primary: #3B82F6;
  --font-size-base: 16px;
  --spacing-unit: 8px;
}
```

**Анализ:**
```bash
npm run mcp:design-tokens
```

---

## E

### ES Module
**ES модуль** - стандарт JavaScript модулей (import/export).

**Отличия от CommonJS:**
```javascript
// CommonJS (старый)
const fs = require('fs');
module.exports = { ... };

// ES Module (новый)
import fs from 'fs';
export default { ... };
```

**Важно для MCP:** MCP server использует ES modules (`"type": "module"` в package.json).

---

## G

### Git Hotspots
**Горячие точки Git** - файлы которые изменяются чаще всего (высокая churn rate).

**Интерпретация:**
- Высокий churn = либо активная разработка, либо проблемный код
- Комбинация высокий churn + высокая сложность = технический долг

**Команда:**
```bash
npm run mcp:git-hotspots
```

---

### Grade
**Оценка** - буквенная оценка качества кода (A-F).

**Mapping:**
| Score | Grade | Описание |
|-------|-------|----------|
| 90-100 | A | Отлично |
| 80-89 | B | Хорошо |
| 70-79 | C | Удовлетворительно |
| 60-69 | D | Плохо |
| < 60 | F | Провал |

---

## I

### i18n (Internationalization)
**Интернационализация** - поддержка множественных языков в приложении.

**Анализ:**
```bash
npm run mcp:i18n
```

**Проверяет:**
- Неиспользуемые ключи переводов
- Отсутствующие переводы
- Hardcoded строки (не вынесены в i18n)

---

## M

### MCP (Model Context Protocol)
**Model Context Protocol** - система инструментов для анализа кода с экономией токенов 90-98%.

**Альтернатива:** Read/Grep/Glob (старый подход, 15,000+ токенов)

**Преимущество:**
- ✅ Экономия токенов (90-98%)
- ✅ Кэширование результатов
- ✅ Структурированный вывод
- ✅ AST parsing (точнее regex)

---

### Mock Generator
**Генератор моков** - автоматическое создание mock данных для тестирования.

**Команда:**
```bash
npm run mcp:mock-generator -- Device
```

**Результат:**
```typescript
export const mockDevice = {
  id: '123e4567-e89b-12d3-a456-426614174000',
  name: 'Test Device',
  model: 'MCT-100',
  // ...
};
```

---

## P

### Project Health
**Здоровье проекта** - комплексная оценка качества кодовой базы.

**Три столпа:**
1. Security (безопасность)
2. Accessibility (доступность)
3. Tech Debt (технический долг)

**Команда:**
```bash
npm run mcp:full-project-audit
```

---

## R

### Re-renders
**Повторные рендеры** - React компонент перерисовывается чаще чем нужно.

**Причины:**
- Отсутствие `useMemo`/`useCallback`
- Неправильное использование state
- Излишние prop changes

**Анализ:**
```bash
npm run mcp:rerenders-detection
```

---

## S

### Security Audit
**Аудит безопасности** - проверка кода на уязвимости.

**Проверяет:**
- XSS (Cross-Site Scripting)
- SQL Injection
- Command Injection
- Path Traversal
- Hardcoded secrets
- Insecure dependencies

**Команда:**
```bash
npm run mcp:security-audit
```

**Результат:**
- Critical vulnerabilities (блокеры)
- High priority (нужно исправить)
- Medium/Low (рекомендации)

---

### State Management
**Управление состоянием** - анализ как приложение управляет state (useState, Zustand, Redux).

**Анализ:**
```bash
npm run mcp:state-management
```

**Проверяет:**
- Какие state managers используются
- Количество state в компонентах
- Props drilling (передача props через 3+ уровня)
- Global vs Local state

---

## T

### Tech Debt
**Технический долг** - накопленные проблемы в коде требующие рефакторинга.

**Индикаторы:**
- TODO/FIXME комментарии
- Deprecated APIs
- Дублированный код
- Высокая сложность
- Отсутствие тестов

**Команда:**
```bash
npm run mcp:tech-debt
```

**Результат:**
- Количество TODO/FIXME
- Deprecated usage
- Code smells
- Prioritized list

---

### Token Savings
**Экономия токенов** - разница между количеством токенов при использовании Read/Grep vs MCP.

**Пример:**
```
Without MCP: 15,000 tokens
With MCP: 800 tokens
Savings: 14,200 tokens (94.7%)
```

**Средняя экономия MCP:** 90-98%

---

### TTL (Time To Live)
**Время жизни кэша** - сколько секунд результат MCP инструмента хранится в кэше.

**Значения по умолчанию:**
```typescript
{
  gitBased: 5 * 60,      // 5 минут (300s)
  audits: 30 * 60,       // 30 минут (1800s)
  analysis: 2 * 60 * 60  // 2 часа (7200s)
}
```

**Почему разные TTL:**
- Git-based: Код часто меняется → короткий TTL
- Audits: Долгие проверки → длинный TTL
- Analysis: Средние по скорости → средний TTL

---

## U

### Unused Exports
**Неиспользуемые экспорты** - функции/компоненты которые экспортируются но нигде не импортируются.

**Команда:**
```bash
npm run mcp:unused
```

**Результат:**
```
src/utils/oldHelper.ts:
  - export function oldFunc() [UNUSED]

src/components/OldButton.tsx:
  - export const OldButton [UNUSED]
```

**Действие:** Удалить или перестать экспортировать.

---

## V

### Vulnerability
**Уязвимость** - проблема безопасности в коде.

**Severity levels:**
- 🔴 **Critical** - немедленно исправить (XSS, SQL injection)
- 🟠 **High** - исправить в течение недели
- 🟡 **Medium** - исправить в ближайшем релизе
- 🟢 **Low** - рекомендация

**Где искать:** `npm run mcp:security-audit`

---

## W

### Workflow
**Рабочий процесс** - последовательность действий для достижения цели.

**Пример: Pre-commit workflow**
```bash
1. npm run mcp:quick-health      # 2s - быстрая проверка
2. npm run mcp:security-audit    # 5s - безопасность
3. npm run mcp:unused            # 1s - dead code
4. git commit -m "feat: ..."     # коммит если все ОК
```

---

## Сокращения

- **A11y** = Accessibility (доступность) - "a" + 11 букв + "y"
- **i18n** = Internationalization - "i" + 18 букв + "n"
- **XSS** = Cross-Site Scripting
- **AST** = Abstract Syntax Tree
- **TTL** = Time To Live
- **OOM** = Out Of Memory
- **CI/CD** = Continuous Integration / Continuous Deployment

---

## Единицы измерения

### Токены
**Token** - единица текста для языковых моделей (≈4 символа или ≈0.75 слова).

**Примеры:**
- "hello" = 1 token
- "console.log" = 3 tokens
- Средний файл (200 строк) ≈ 1,500 tokens

### Время
**Execution time** - время выполнения MCP команды.

**Категории:**
- < 1s: Мгновенно (cache hit)
- 1-3s: Быстро (git-based tools)
- 3-10s: Средне (audits)
- 10-20s: Медленно (composite skills)

### Размер кода
**Lines of Code (LOC)** - количество строк кода.

**Категории проектов:**
- Small: < 5,000 LOC
- Medium: 5,000-20,000 LOC
- Large: 20,000-100,000 LOC
- Enterprise: > 100,000 LOC

---

## См. также

- **[FAQ.md](FAQ.md)** - Частые вопросы
- **[TROUBLESHOOTING.md](TROUBLESHOOTING.md)** - Решение проблем
- **[CHEATSHEET.md](CHEATSHEET.md)** - Все команды
- **[USAGE_GUIDE.md](USAGE_GUIDE.md)** - Детальные примеры

---

**Версия:** 1.0.0
**Последнее обновление:** 2025-11-10
**Всего терминов:** 42
