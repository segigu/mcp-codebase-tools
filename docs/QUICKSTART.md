# MCP Tools - Quick Start Guide

**Экономия токенов: 90-98% на каждой задаче!**

---

## ⚡ 3-минутный старт

### Шаг 1: Setup (1 минута)

```bash
npm run mcp:setup
```

Что происходит:
- ✅ Создаются git hooks для автоматических проверок
- ✅ Настраиваются helper scripts
- ✅ Создаются директории для отчетов
- ✅ Тестируются MCP инструменты

### Шаг 2: Попробуй основные команды (1 минута)

```bash
# Структура проекта
npm run mcp:structure

# Найти где используется компонент
npm run mcp:find-imports -- Button

# Быстрая проверка здоровья
npm run mcp:quick-health
```

### Шаг 3: Интегрируй с Claude Code (1 минута)

1. Открой `docs/CLAUDE_MCP_RULES_SNIPPET.md`
2. Скопируй всё содержимое
3. Добавь в `CLAUDE.md` после секции "КРИТИЧЕСКИЕ ПРАВИЛА"

**Done!** Claude теперь автоматически использует MCP вместо Read/Grep.

---

## 🎯 Самые полезные команды

### Ежедневное использование

```bash
# Начало дня - быстрая проверка
npm run mcp:quick-health

# Найти где используется символ
npm run mcp:find-imports -- <name>

# Найти кто вызывает функцию
npm run mcp:callers -- <function>
```

### Перед коммитом

```bash
# Полная проверка кода
npm run mcp:code-health-check

# Security scan
npm run mcp:security-audit

# Dead code check
npm run mcp:unused
```

### Перед релизом

```bash
# Полный аудит проекта
npm run mcp:full-project-audit

# Обновить документацию
npm run mcp:project-docs
```

---

## 🤖 Claude Code автоматизация

После добавления snippet в `CLAUDE.md`:

**User:** "Где используется Button?"

**Claude:**
```
*АВТОМАТИЧЕСКИ запускает*
npm run mcp:find-imports -- Button

Результат: 23 файла
Экономия: 15,000 → 800 токенов (95%)
```

❌ **БЕЗ MCP:** Claude читает 23 файла вручную → 15,000 токенов
✅ **С MCP:** Claude запускает команду → 800 токенов

**Экономия: 95%!**

---

## 📚 Полная документация

| Документ | Для чего | Время |
|----------|----------|-------|
| [docs/MCP_CHEATSHEET.md](docs/MCP_CHEATSHEET.md) | Шпаргалка по всем 30 командам | 10 мин |
| [docs/MCP_AUTO_INTEGRATION.md](docs/MCP_AUTO_INTEGRATION.md) | Полное руководство по автоматизации | 30 мин |
| [docs/MCP_README.md](docs/MCP_README.md) | Навигация по всей документации | 5 мин |

---

## 🎓 Примеры использования

### Пример 1: Рефакторинг компонента

```bash
# 1. Найти где используется
npm run mcp:find-imports -- OldComponent
# → Найдено в 12 файлах

# 2. Найти кто вызывает методы
npm run mcp:callers -- handleOldMethod
# → Вызывается из 5 мест

# 3. Рефакторить (теперь безопасно!)

# 4. Проверить не сломалось ли
npm run mcp:find-usages -- NewComponent
```

**Экономия:** ~30,000 → 1,500 токенов (95%)

---

### Пример 2: Анализ нового проекта

```bash
# 1. Общая структура
npm run mcp:structure

# 2. Какие компоненты есть
npm run mcp:component-inventory

# 3. Какие API endpoints
npm run mcp:api-inventory

# 4. Качество кода
npm run mcp:code-health-check

# 5. Сгенерировать документацию
npm run mcp:project-docs
```

**Экономия:** ~150,000 → 8,000 токенов (95%)

---

### Пример 3: Daily workflow

```bash
# Утро - проверка здоровья
npm run mcp:quick-health

# Работа - использование find/callers по мере надобности
npm run mcp:find-imports -- <component>

# Вечер - перед коммитом
git commit  # Git hooks автоматически запустят проверки

# Еженедельно
npm run mcp:weekly-trends
```

---

## 🎯 Композитные навыки (NEW!)

### 3 супер-мощные команды

```bash
# 1. Полный аудит проекта
npm run mcp:full-project-audit
# Запускает: Security + A11y + Tech Debt
# Экономия: 100,000 → 5,000 токенов (95%)
# Время: ~10 секунд
# Результат: Grade A-F, приоритизированные issues

# 2. Проверка здоровья кода
npm run mcp:code-health-check
# Запускает: Complexity + Dead Code + Performance + State
# Экономия: 80,000 → 4,000 токенов (95%)
# Время: ~8 секунд
# Результат: Health metrics, quick wins

# 3. Генерация документации
npm run mcp:project-docs
# Запускает: Components + API + i18n
# Экономия: 60,000 → 3,000 токенов (95%)
# Время: ~5 секунд
# Результат: docs/GENERATED_DOCUMENTATION.md
```

---

## 💾 Кэширование

**Первый запуск:** 2-3 секунды
**Повторный:** 100-200ms (из кэша!)

```bash
# Проверить статус кэша
npm run mcp:cache-status

# Очистить кэш (если нужно)
npm run mcp:cache-clear
```

**TTL (время жизни кэша):**
- Git-based: 5 минут
- Audits: 30 минут
- Analysis: 2 часа

---

## 🔧 Troubleshooting

### Команда зависла
```bash
npm run mcp:cache-clear
```

### Странные результаты
```bash
npm run mcp:cache-clear -- <tool-name>
```

### Git hooks блокируют коммит
```bash
git commit --no-verify  # Временно пропустить
```

---

## 📊 Отслеживание эффективности

```bash
# Cache hit rate (цель: >70%)
npm run mcp:cache-status | grep "Hit Rate"

# Code health (цель: Grade B+)
npm run mcp:quick-health | grep "grade:"

# Audit stats
npm run mcp:audit-summary
```

---

## ❓ FAQ

**Q: Нужны ли дополнительные зависимости?**
A: Нет! Все работает из коробки.

**Q: Как часто запускать audits?**
A: Daily - `mcp:quick-health`, Weekly - `mcp:full-project-audit`

**Q: Можно ли отключить git hooks?**
A: Да: `git commit --no-verify` или `rm .husky/pre-commit-mcp`

**Q: Где хранятся результаты?**
A:
- Audit log: `docs/audits/AUDIT_LOG.json`
- Cache: `docs/cache/MCP_CACHE.json`
- Reports: `docs/reports/`

---

## 🎉 Что дальше?

### Уровень 1: Базовый (сейчас)
- ✅ Setup завершен
- ✅ Основные команды работают
- ✅ Claude Code интегрирован

### Уровень 2: Продвинутый
- [ ] Настрой CI/CD ([docs/MCP_AUTO_INTEGRATION.md#5](docs/MCP_AUTO_INTEGRATION.md))
- [ ] Настрой daily reports ([docs/MCP_AUTO_INTEGRATION.md#4](docs/MCP_AUTO_INTEGRATION.md))
- [ ] Добавь Slack notifications ([docs/MCP_AUTO_INTEGRATION.md#7.3](docs/MCP_AUTO_INTEGRATION.md))

### Уровень 3: Профессиональный
- [ ] Dashboard с метриками
- [ ] Автоматический мониторинг
- [ ] Интеграция с Jira/Linear
- [ ] Custom workflows

**Гайды:** [docs/MCP_AUTO_INTEGRATION.md](docs/MCP_AUTO_INTEGRATION.md)

---

## 📞 Помощь

- **Документация:** [docs/MCP_README.md](docs/MCP_README.md)
- **Cheat Sheet:** [docs/MCP_CHEATSHEET.md](docs/MCP_CHEATSHEET.md)
- **Full Guide:** [docs/MCP_AUTO_INTEGRATION.md](docs/MCP_AUTO_INTEGRATION.md)
- **Проблемы:** `npm run mcp:help`

---

## 🚀 Итог

**У тебя теперь есть:**
- ✅ 30 MCP инструментов
- ✅ Экономия токенов 90-98%
- ✅ Автоматические git hooks
- ✅ Claude Code обучен
- ✅ Кэширование для скорости

**Главное правило:**
> ВСЕГДА используй MCP вместо Read/Grep для анализа кода!

**Экономия токенов: 90-98%** 🎯

---

*Quick Start версия: 1.0.0*
*Полная документация: [docs/MCP_README.md](docs/MCP_README.md)*
