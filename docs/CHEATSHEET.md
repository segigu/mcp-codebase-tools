# MCP Tools - Шпаргалка (Cheat Sheet)

**Быстрый справочник по всем 30 MCP командам**

---

## ⚡ Quick Reference Table

| Команда | Что делает | Экономия токенов |
|---------|-----------|------------------|
| `mcp:find-imports` | Где импортируется компонент/символ | 95% (15K → 800) |
| `mcp:find-usages` | Где используется символ/функция | 97% (14K → 600) |
| `mcp:callers` | Кто вызывает функцию | 95% (12K → 600) |
| `mcp:find-components` | Список всех React компонентов | 94% (14K → 800) |
| `mcp:structure` | Структура проекта | 95% (8K → 400) |
| `mcp:unused` | Неиспользуемые экспорты | 92% (5K → 400) |
| `mcp:complexity` | Сложность кода | 92% (6K → 500) |
| `mcp:security-audit` | Проверка безопасности | 98% (100K → 2K) |
| `mcp:a11y-audit` | Проверка доступности | 98% (80K → 1.8K) |
| `mcp:tech-debt` | Технический долг | 95% (20K → 1K) |
| `mcp:full-project-audit` | **Полный аудит** (Security+A11y+TechDebt) | 95% (100K → 5K) |
| `mcp:code-health-check` | **Здоровье кода** (Complexity+Unused+Perf) | 95% (80K → 4K) |
| `mcp:project-docs` | **Генерация документации** (Components+API+i18n) | 95% (60K → 3K) |

**💡 Совет:** Используй `npm run mcp:help` для полного списка команд с описаниями

---

## 🚀 Основные команды (Most Used)

### Поиск и навигация
```bash
# Где импортируется компонент/функция
npm run mcp:find-imports -- Button
# Экономия: 15,000 → 800 токенов (95%)

# Где используется символ
npm run mcp:find-usages -- useState
# Экономия: 13,800 → 600 токенов (97%)

# Кто вызывает функцию (обратные ссылки)
npm run mcp:callers -- handleSubmit
# Экономия: 12,000 → 600 токенов (95%)

# Список всех компонентов
npm run mcp:find-components
# Экономия: 14,000 → 800 токенов (94%)

# Структура проекта
npm run mcp:structure
# Экономия: 8,000 → 400 токенов (95%)
```

---

## 🏥 Качество и аудит

### Проверки кода
```bash
# Неиспользуемые экспорты (dead code)
npm run mcp:unused
# Экономия: 5,000 → 400 токенов (92%)

# Цикломатическая сложность
npm run mcp:complexity
# Экономия: 6,000 → 500 токенов (92%)

# Security audit
npm run mcp:security-audit
# Экономия: 100,000 → 2,000 токенов (98%)

# Accessibility audit
npm run mcp:a11y-audit
# Экономия: 80,000 → 1,800 токенов (98%)

# Technical debt
npm run mcp:tech-debt
# Экономия: 20,000 → 1,000 токенов (95%)
```

---

## 🎨 Дизайн и UI

```bash
# Инвентаризация компонентов
npm run mcp:component-inventory
# Экономия: 30,000 → 1,500 токенов (95%)

# Design tokens анализ
npm run mcp:design-tokens
# Экономия: 15,000 → 800 токенов (95%)

# Оптимизация Tailwind
npm run mcp:tailwind-optimizer
# Экономия: 12,000 → 700 токенов (94%)
```

---

## ⚡ Performance

```bash
# Bundle size анализ
npm run mcp:bundle-analysis
# Экономия: 10,000 → 600 токенов (94%)

# Re-renders detection
npm run mcp:rerenders-detection
# Экономия: 40,000 → 1,200 токенов (97%)

# State management анализ
npm run mcp:state-management
# Экономия: 40,000 → 1,200 токенов (97%)
```

---

## 🧪 Тестирование

```bash
# Test coverage gaps
npm run mcp:test-coverage-gaps
# Экономия: 25,000 → 1,200 токенов (95%)

# Mock данные генератор
npm run mcp:mock-generator -- Device
# Экономия: 2,000 → 200 токенов (90%)
```

---

## 🌐 i18n и API

```bash
# Анализ локализации
npm run mcp:i18n
# Экономия: 18,000 → 900 токенов (95%)

# API endpoints инвентаризация
npm run mcp:api-inventory
# Экономия: 20,000 → 1,000 токенов (95%)
```

---

## 📚 Документация

```bash
# Генерация документации
npm run mcp:docs-generator -- LoginForm
# Экономия: 5,000 → 500 токенов (90%)

# Git hotspots анализ
npm run mcp:git-hotspots
# Экономия: 8,000 → 500 токенов (94%)
```

---

## 🎯 Композитные навыки (NEW!)

### Самые мощные команды - объединяют несколько инструментов

```bash
# Полный аудит проекта
# (Security + A11y + Tech Debt)
npm run mcp:full-project-audit
# Экономия: 100,000 → 5,000 токенов (95%)
# Время: ~10-15 секунд

# Проверка здоровья кода
# (Complexity + Dead Code + Performance + State)
npm run mcp:code-health-check
# Экономия: 80,000 → 4,000 токенов (95%)
# Время: ~8-12 секунд

# Генерация полной документации
# (Components + API + i18n)
npm run mcp:project-docs
# Экономия: 60,000 → 3,000 токенов (95%)
# Время: ~5-8 секунд
# Выход: docs/GENERATED_DOCUMENTATION.md
```

---

## 🔧 Служебные команды

### Управление инструментами
```bash
# Список всех инструментов (краткий)
npm run mcp:list

# Описание инструментов (средний)
npm run mcp:describe

# Полные схемы инструментов
npm run mcp:schema -- find-imports

# Помощь
npm run mcp:help
```

### Кэш
```bash
# Статус кэша
npm run mcp:cache-status

# Очистить весь кэш
npm run mcp:cache-clear

# Очистить кэш инструмента
npm run mcp:cache-clear -- securityAudit
```

### Audit Log
```bash
# История аудитов инструмента
npm run mcp:audit-history -- securityAudit

# Общая статистика аудитов
npm run mcp:audit-summary
```

---

## 📊 Утилиты (Helper Scripts)

```bash
# Быстрая проверка здоровья проекта
npm run mcp:quick-health

# Ежедневный отчет
npm run mcp:daily-report

# Недельные тренды
npm run mcp:weekly-trends
```

---

## 🎓 Паттерны использования

### Паттерн 1: Рефакторинг компонента
```bash
# 1. Найти где используется
npm run mcp:find-imports -- OldComponent

# 2. Найти кто вызывает
npm run mcp:callers -- handleOldMethod

# 3. Рефакторить

# 4. Проверить не сломалось ли
npm run mcp:find-usages -- NewComponent
```

### Паттерн 2: Проверка перед коммитом
```bash
# 1. Быстрая проверка
npm run mcp:quick-health

# 2. Security scan
npm run mcp:security-audit

# 3. Dead code check
npm run mcp:unused

# 4. Commit (git hooks запустят дополнительные проверки)
git commit -m "feat: ..."
```

### Паттерн 3: Подготовка к релизу
```bash
# 1. Полный аудит
npm run mcp:full-project-audit

# 2. Проверка кода
npm run mcp:code-health-check

# 3. Обновить документацию
npm run mcp:project-docs

# 4. Review результатов
cat docs/GENERATED_DOCUMENTATION.md
cat docs/audits/AUDIT_LOG.json
```

### Паттерн 4: Анализ нового проекта
```bash
# 1. Структура
npm run mcp:structure

# 2. Компоненты
npm run mcp:component-inventory

# 3. API endpoints
npm run mcp:api-inventory

# 4. Качество кода
npm run mcp:code-health-check

# 5. Сгенерировать документацию
npm run mcp:project-docs
```

---

## ⏱️ Производительность

| Категория | Первый запуск | Из кэша | TTL |
|-----------|---------------|---------|-----|
| Git-based | 2-3s | 100ms | 5 min |
| Audits | 5-10s | 200ms | 30 min |
| Analysis | 3-5s | 150ms | 2 hours |
| Composite | 10-15s | - | varies |

**Кэш хранится в:** `docs/cache/MCP_CACHE.json`

---

## 🎯 Quick Wins

### Самые быстрые команды (< 1 секунда из кэша):
- `mcp:find-imports`
- `mcp:find-usages`
- `mcp:callers`
- `mcp:structure`
- `mcp:cache-status`

### Самые ценные команды (максимум инсайтов):
- `mcp:full-project-audit` 🏆
- `mcp:code-health-check` 🏆
- `mcp:component-inventory` 🏆
- `mcp:security-audit`
- `mcp:project-docs`

### Лучшее соотношение время/ценность:
1. `mcp:quick-health` - 2s, дает общую картину
2. `mcp:find-usages` - 0.5s, критично для рефакторинга
3. `mcp:unused` - 1s, быстро находит dead code

---

## 💡 Pro Tips

1. **Используй Tab completion:**
   ```bash
   npm run mcp:<TAB>  # Покажет все доступные команды
   ```

2. **Параллельные запуски:**
   ```bash
   npm run mcp:security-audit & npm run mcp:a11y-audit & wait
   ```

3. **Фильтрация выхода:**
   ```bash
   npm run mcp:complexity | grep "high"
   npm run mcp:unused | head -20
   ```

4. **Сохранение результатов:**
   ```bash
   npm run mcp:full-project-audit > reports/audit-$(date +%F).log
   ```

5. **Пайплайны:**
   ```bash
   npm run mcp:find-imports -- Button | \
     grep "\.tsx" | \
     xargs -I {} npm run mcp:complexity -- {}
   ```

6. **Watch mode (мониторинг):**
   ```bash
   watch -n 60 'npm run mcp:quick-health'
   ```

---

## 🚨 Troubleshooting

### Команда зависла
```bash
# Очисти кэш
npm run mcp:cache-clear

# Проверь процессы
ps aux | grep mcp-wrapper
```

### Странные результаты
```bash
# Очисти кэш инструмента
npm run mcp:cache-clear -- <tool-name>

# Перезапусти
npm run mcp:<command>
```

### Out of memory
```bash
# Используй limit
npm run mcp:complexity -- --limit 100

# Или очисти большой кэш
npm run mcp:cache-clear
```

---

## 📚 Дополнительно

- **Полная документация:** [docs/MCP_AUTO_INTEGRATION.md](MCP_AUTO_INTEGRATION.md)
- **Setup автоматизации:** `bash scripts/setup-mcp-automation.sh`
- **Tool registry:** `npm run mcp:list`
- **Audit log:** `docs/audits/AUDIT_LOG.json`

---

## 🎓 Обучение Claude Code

**Добавь в CLAUDE.md:**
```markdown
ВСЕГДА проверяй "Могу ли использовать MCP?" ПЕРЕД Read/Grep!
Экономия токенов: 90-98% на каждой задаче!
```

**См:** [docs/CLAUDE_MCP_RULES_SNIPPET.md](CLAUDE_MCP_RULES_SNIPPET.md)

---

**Версия:** 1.0.0
**Последнее обновление:** 2025-01-09
**Всего инструментов:** 30 (27 базовых + 3 композитных)
