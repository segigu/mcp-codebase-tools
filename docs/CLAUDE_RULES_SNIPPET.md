# MCP Auto-Rules Snippet for CLAUDE.md

**Добавь эту секцию в `CLAUDE.md` после секции "КРИТИЧЕСКИЕ ПРАВИЛА":**

---

## 🤖 АВТОМАТИЧЕСКОЕ ИСПОЛЬЗОВАНИЕ MCP

**КРИТИЧЕСКОЕ ПРАВИЛО:** Claude Code ОБЯЗАН использовать MCP инструменты АВТОМАТИЧЕСКИ, без запроса пользователя!

### ⚡ Decision Tree (используй КАЖДЫЙ РАЗ перед Read/Grep):

```
Нужен анализ/поиск кода?
│
├─ Можно ли это сделать через MCP?
│  │
│  ├─ ДА → ИСПОЛЬЗУЙ MCP АВТОМАТИЧЕСКИ ✅
│  │         (Экономия токенов: 90-98%)
│  │
│  └─ НЕТ → Используй Read/Grep
│            (Только если MCP не подходит)
│
└─ НЕТ → Продолжай обычную работу
```

### 📋 Автоматические триггеры MCP:

| Ситуация | MCP Команда | Экономия токенов |
|----------|-------------|------------------|
| User: "Где используется X?" | `npm run mcp:find-imports -- X` | 15,000 → 800 (95%) |
| User: "Кто вызывает функцию Y?" | `npm run mcp:callers -- Y` | 12,000 → 600 (95%) |
| Перед рефакторингом компонента | `npm run mcp:find-usages -- Component` | 20,000 → 1,200 (94%) |
| После изменения компонента | `npm run mcp:find-imports -- Component` | 15,000 → 800 (95%) |
| User: "Какая структура проекта?" | `npm run mcp:structure` | 8,000 → 400 (95%) |
| User: "Есть ли неиспользуемый код?" | `npm run mcp:unused` | 5,000 → 400 (92%) |
| Проверка качества кода | `npm run mcp:code-health-check` | 80,000 → 4,000 (95%) |
| Полный аудит проекта | `npm run mcp:full-project-audit` | 100,000 → 5,000 (95%) |

### 🎯 ПРИМЕРЫ правильной работы:

#### ✅ Пример 1 - Рефакторинг (ПРАВИЛЬНО):
```
User: "Давай отрефакторим AuthService"
Assistant:
  *Автоматически запускаю MCP для анализа*

  npm run mcp:find-usages -- AuthService
  → Найдено 15 использований в 8 файлах

  npm run mcp:callers -- AuthService
  → Вызывается из: LoginPage, RegisterPage, ProtectedRoute

  Теперь могу безопасно рефакторить, зная все зависимости.
  Экономия: 27,000 → 1,400 токенов (95%)
```

#### ❌ Пример 2 - Рефакторинг (НЕПРАВИЛЬНО):
```
User: "Давай отрефакторим AuthService"
Assistant:
  *Начинает читать файлы вручную*

  Read src/services/AuthService.ts
  Read src/pages/LoginPage.tsx
  Read src/pages/RegisterPage.tsx
  ... еще 10 файлов ...

  Потрачено: 27,000 токенов ЗРЯ ❌
```

### 🔄 Workflow паттерны:

**1. Начало сессии (автоматически):**
```bash
npm run mcp:quick-health  # Проверка здоровья проекта
```

**2. Перед любым рефакторингом:**
```bash
npm run mcp:find-usages -- <symbol>   # Где используется
npm run mcp:callers -- <function>     # Кто вызывает
```

**3. После изменений компонента:**
```bash
npm run mcp:find-imports -- <Component>  # Показать затронутые файлы
```

**4. Перед коммитом (автоматически через git hooks):**
```bash
npm run mcp:security-audit      # Проверка безопасности
npm run mcp:unused              # Проверка мертвого кода
```

**5. Перед релизом:**
```bash
npm run mcp:full-project-audit  # Полный аудит
npm run mcp:project-docs        # Обновить документацию
```

### 🚫 Когда НЕ использовать MCP:

- ✅ Чтение ОДНОГО конкретного файла для редактирования
- ✅ Чтение конфигов (package.json, tsconfig.json)
- ✅ Чтение документации (README.md)
- ❌ ВСЕ ОСТАЛЬНЫЕ случаи - ИСПОЛЬЗУЙ MCP!

### 💾 Кэширование = скорость:

- Первый запуск: ~2-3 секунды
- Повторный запуск: ~100-200ms (из кэша!)
- TTL: 5min (git) / 30min (audit) / 2hrs (i18n)
- **НЕ БОЙСЯ переиспользовать команды - они мгновенные!**

### 📊 Композитные навыки (multiple tools в 1 команде):

```bash
# Вместо запуска 3 аудитов отдельно
npm run mcp:full-project-audit
# Запускает: security + a11y + tech-debt за ~5 секунд

# Вместо 4 анализов кода отдельно
npm run mcp:code-health-check
# Запускает: complexity + unused + rerenders + state за ~4 секунды

# Вместо 3 инвентаризаций отдельно
npm run mcp:project-docs
# Запускает: components + api + i18n + генерирует markdown
```

### ⚙️ Автоматизация (уже настроено):

✅ **Git hooks:** Автоматические проверки при коммите
✅ **Кэширование:** Мгновенные повторные запуски
✅ **Audit log:** Автоматическое логирование всех проверок
✅ **Тренды:** Отслеживание улучшений/регрессий

**Запусти setup:**
```bash
bash scripts/setup-mcp-automation.sh
```

### 📖 Полная документация:

- **Quick Start:** [docs/MCP_AUTO_INTEGRATION.md](docs/MCP_AUTO_INTEGRATION.md)
- **Tool Reference:** `npm run mcp:help`
- **Cache Status:** `npm run mcp:cache-status`
- **Audit History:** `npm run mcp:audit-summary`

---

**🎯 ГЛАВНОЕ ПРАВИЛО:**

> **ВСЕГДА проверяй "Могу ли я использовать MCP?" ПЕРЕД использованием Read/Grep!**
>
> **Экономия токенов: 90-98% на каждой задаче!**

---
