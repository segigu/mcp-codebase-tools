# MCP Tools - Frequently Asked Questions (FAQ)

**Ответы на 20 самых частых вопросов по MCP**

---

## 📚 Основы (Basics)

### 1. Что такое MCP?

**MCP (Model Context Protocol)** - это система инструментов для анализа кода, которая экономит 90-98% токенов при работе с Claude Code.

Вместо того чтобы читать десятки файлов через Read/Grep (15,000+ токенов), MCP запускает специализированные инструменты, которые возвращают только нужную информацию (~500-800 токенов).

**Пример:**
- Без MCP: `Grep "Button" src/**/*.tsx` → 15,000 токенов (читает все файлы)
- С MCP: `npm run mcp:find-imports -- Button` → 800 токенов (98% экономия)

---

### 2. Почему MCP экономит столько токенов?

**Три причины:**

1. **Целевой анализ:** Вместо чтения всего файла, MCP читает только нужные части (AST parsing)
2. **Кэширование:** Результаты кэшируются на 5 минут - 2 часа (зависит от типа инструмента)
3. **Структурированный вывод:** Возвращаются только метаданные, а не весь исходный код

**Сравнение:**
```bash
# Read/Grep подход (старый)
Read src/page1.tsx    # 3,000 токенов
Read src/page2.tsx    # 4,000 токенов
Read src/page3.tsx    # 5,000 токенов
Grep "Button" src/    # 3,000 токенов
# Итого: 15,000 токенов

# MCP подход (новый)
npm run mcp:find-imports -- Button
# Возвращает: ["src/page1.tsx", "src/page2.tsx", "src/page3.tsx"]
# Итого: 800 токенов (95% экономия!)
```

---

### 3. Когда использовать MCP вместо Read/Grep?

**ВСЕГДА используй MCP для:**
- ✅ Поиска где используется компонент/функция/хук
- ✅ Получения списка компонентов
- ✅ Анализа структуры проекта
- ✅ Поиска неиспользуемого кода
- ✅ Анализа сложности кода
- ✅ Аудитов безопасности и доступности
- ✅ Управления задачами (add/list/done)
- ✅ Любого анализа требующего чтения 3+ файлов

**Можно использовать Read для:**
- ✅ Чтения КОНКРЕТНОГО файла для редактирования (1 файл)
- ✅ Чтения конфигов (package.json, tsconfig.json)
- ✅ Чтения документации (README.md, CLAUDE.md)

---

### 4. Какие есть категории MCP инструментов?

**30 инструментов в 7 категориях:**

1. **Анализ кода** (6): find-imports, find-usages, callers, complexity, unused, structure
2. **Аудит** (4): security-audit, a11y-audit, tech-debt, test-coverage-gaps
3. **Инвентаризация** (5): find-components, component-inventory, api-inventory, design-tokens, i18n
4. **Performance** (3): bundle-analysis, rerenders-detection, state-management
5. **Тестирование** (2): test-coverage-gaps, mock-generator
6. **Документация** (2): docs-generator, git-hotspots
7. **Composite Skills** (3): full-project-audit, code-health-check, project-docs

**См. полный список:** [CHEATSHEET.md](CHEATSHEET.md)

---

### 5. Что такое Composite Skills?

**Composite Skills** - это "супер-команды", объединяющие несколько инструментов в одну.

**Три главные:**

```bash
# 1. Full Project Audit (Security + A11y + Tech Debt)
npm run mcp:full-project-audit
# Экономия: 100,000 → 5,000 токенов (95%)
# Время: ~10-15 секунд
# Результат: Grade A-F + prioritized issues

# 2. Code Health Check (Complexity + Unused + Performance)
npm run mcp:code-health-check
# Экономия: 80,000 → 4,000 токенов (95%)
# Время: ~8-12 секунд

# 3. Project Docs (Components + API + i18n)
npm run mcp:project-docs
# Экономия: 60,000 → 3,000 токенов (95%)
# Время: ~5-8 секунд
# Выход: docs/GENERATED_DOCUMENTATION.md
```

---

## 🚀 Использование (Usage)

### 6. Как найти где используется компонент?

```bash
# Где импортируется компонент
npm run mcp:find-imports -- Button
# Экономия: 15,000 → 800 токенов (95%)

# Где используется символ (функция/хук)
npm run mcp:find-usages -- useState
# Экономия: 13,800 → 600 токенов (97%)

# Кто вызывает функцию
npm run mcp:callers -- handleSubmit
# Экономия: 12,000 → 600 токенов (95%)
```

---

### 7. Как получить список всех компонентов?

```bash
# Быстрый список (только пути)
npm run mcp:find-components
# Экономия: 14,000 → 800 токенов (94%)

# Детальная инвентаризация (props, state, hooks)
npm run mcp:component-inventory
# Экономия: 30,000 → 1,500 токенов (95%)
```

---

### 8. Как проверить качество кода перед коммитом?

```bash
# Быстрая проверка (2 секунды)
npm run mcp:quick-health

# Полная проверка (10 секунд)
npm run mcp:code-health-check

# Комплексная проверка (15 секунд)
npm run mcp:full-project-audit
```

**Рекомендованный workflow:**
```bash
# 1. Быстрая проверка
npm run mcp:quick-health

# 2. Если нашлись проблемы - детальный анализ
npm run mcp:security-audit
npm run mcp:unused
npm run mcp:complexity

# 3. Исправить проблемы

# 4. Финальная проверка
npm run mcp:full-project-audit

# 5. Коммит
git commit -m "feat: ..."
```

---

### 9. Как управлять задачами через MCP?

```bash
# Добавить задачу (94% экономия vs агент!)
npm run mcp:task-add -- "Исправить баг с авторизацией"
# Экономия: 9,000 → 500 токенов
# Скорость: 10-20 секунд → 1 секунда (10-20x быстрее!)

# Список задач
npm run mcp:task-list              # Все активные
npm run mcp:task-list bug          # Только баги
npm run mcp:task-list high         # Высокий приоритет
npm run mcp:task-list #typescript  # По тегу

# Взять следующую задачу
npm run mcp:task-next
# Экономия: 4,000 → 300 токенов (93%)

# Завершить задачу
npm run mcp:task-done TASK-001
# Экономия: 4,500 → 400 токенов (91%)

# Статистика
npm run mcp:task-stats
```

---

### 10. Как найти неиспользуемый код?

```bash
# Неиспользуемые экспорты (dead code)
npm run mcp:unused
# Экономия: 5,000 → 400 токенов (92%)

# Результат:
# - src/utils/oldHelper.ts: export function oldFunc()
# - src/components/OldButton.tsx: export const OldButton
```

---

## ⚙️ Технические вопросы (Technical)

### 11. Как работает кэширование?

**Кэш хранится в:** `docs/cache/MCP_CACHE.json`

**TTL (Time To Live) по категориям:**

| Категория | TTL | Почему |
|-----------|-----|--------|
| Git-based (find-imports, structure) | 5 минут | Код часто меняется |
| Audits (security, a11y) | 30 минут | Долгие проверки |
| Analysis (complexity, unused) | 2 часа | Средние по скорости |

**Команды управления кэшем:**
```bash
# Статус кэша
npm run mcp:cache-status

# Очистить весь кэш
npm run mcp:cache-clear

# Очистить кэш инструмента
npm run mcp:cache-clear -- securityAudit
```

---

### 12. Что делать если команда зависла?

```bash
# 1. Очистить кэш инструмента
npm run mcp:cache-clear -- <tool-name>

# 2. Проверить процессы
ps aux | grep mcp-wrapper

# 3. Убить зависший процесс
kill <PID>

# 4. Очистить весь кэш
npm run mcp:cache-clear

# 5. Попробовать снова
npm run mcp:<command>
```

---

### 13. Как читать Audit Log?

**Лог хранится в:** `docs/audits/AUDIT_LOG.json`

**Структура:**
```json
{
  "tools": {
    "securityAudit": {
      "executionCount": 5,
      "lastExecuted": "2025-01-09T12:30:00Z",
      "results": [
        {
          "timestamp": "2025-01-09T12:30:00Z",
          "score": "90/100",
          "grade": "A",
          "trends": { ... }
        }
      ]
    }
  }
}
```

**Команды:**
```bash
# История аудитов инструмента
npm run mcp:audit-history -- securityAudit

# Общая статистика
npm run mcp:audit-summary
```

---

### 14. Почему результат отличается от Read/Grep?

**MCP использует AST parsing, а не regex!**

**Read/Grep:**
```bash
Grep "Button" src/  # Найдет ВСЕ упоминания:
# - import { Button }    ✅ импорт
# - // Button component  ❌ комментарий
# - const buttonText     ❌ переменная
# - <Button />           ✅ использование
```

**MCP:**
```bash
npm run mcp:find-imports -- Button  # Найдет ТОЛЬКО импорты:
# - import { Button } from ...  ✅
# Игнорирует комментарии и переменные
```

**Вывод:** MCP точнее, но может пропустить нестандартные случаи.

---

### 15. Можно ли запускать MCP параллельно?

**Да, можно!**

```bash
# Параллельный запуск
npm run mcp:security-audit & \
npm run mcp:a11y-audit & \
npm run mcp:tech-debt & \
wait

# Или используй composite skill (автоматически параллельно)
npm run mcp:full-project-audit
```

**Но:** Некоторые инструменты используют один кэш - могут конфликтовать.

---

## 🔧 Интеграция (Integration)

### 16. Как интегрировать MCP с Claude Code?

**1. Добавь в CLAUDE.md:**

Скопируй содержимое [CLAUDE_RULES_SNIPPET.md](CLAUDE_RULES_SNIPPET.md) и добавь после секции "КРИТИЧЕСКИЕ ПРАВИЛА".

**2. Claude автоматически будет:**
- ✅ Проверять "Можно ли использовать MCP?" перед Read/Grep
- ✅ Автоматически использовать MCP команды
- ✅ Отчитываться об экономии токенов в каждом ответе

**См. подробнее:** [CLAUDE_RULES_SNIPPET.md](CLAUDE_RULES_SNIPPET.md)

---

### 17. Как настроить Git Hooks для MCP?

```bash
# Setup автоматизации (1 команда)
bash scripts/setup-mcp-automation.sh

# Это создаст:
# - .husky/pre-commit: npm run mcp:quick-health
# - .husky/pre-push: npm run mcp:security-audit
```

**Что проверяется автоматически:**
- Pre-commit: Quick health check (2 секунды)
- Pre-push: Security audit (5 секунд)

**Bypass (в экстренных случаях):**
```bash
git commit --no-verify -m "emergency fix"
```

**См. подробнее:** [AUTO_INTEGRATION.md](AUTO_INTEGRATION.md)

---

### 18. Как добавить MCP в CI/CD?

**GitHub Actions пример:**

```yaml
# .github/workflows/mcp-audit.yml
name: MCP Code Quality

on: [push, pull_request]

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm install
      - run: npm run mcp:full-project-audit
      - run: npm run mcp:security-audit
```

**Что проверять в CI:**
- ✅ Security audit (обязательно)
- ✅ A11y audit (для публичных приложений)
- ✅ Tech debt (предупреждения, не блокирует)
- ❌ Full project audit (слишком долго, 15 секунд)

**См. подробнее:** [AUTO_INTEGRATION.md секция 5](AUTO_INTEGRATION.md#5-cicd-интеграция)

---

### 19. Можно ли создать свой MCP инструмент?

**Да!** MCP система расширяемая.

**Структура инструмента:**

```typescript
// mcp-server/tools/analyze/myCustomTool.ts
export async function myCustomTool(args: {
  projectPath: string;
  options?: any;
}) {
  // 1. Получить пути к файлам
  const files = await glob('src/**/*.tsx');

  // 2. Анализ (AST parsing, regex, etc.)
  const results = files.map(file => analyzeFile(file));

  // 3. Вернуть структурированный результат
  return {
    summary: "Brief summary",
    details: results,
    metadata: { ... }
  };
}

// Добавить в index.ts
export { myCustomTool };
```

**Регистрация в package.json:**
```json
{
  "scripts": {
    "mcp:my-tool": "node scripts/mcp-wrapper.js myCustomTool"
  }
}
```

**См. подробнее:** [CODE_EXECUTION.md секция 4](CODE_EXECUTION.md#4-создание-собственных-инструментов)

---

### 20. Как мониторить использование MCP?

**Три уровня мониторинга:**

**1. Cache Status (каждый день):**
```bash
npm run mcp:cache-status | grep "Hit Rate"
# Цель: > 70%
```

**2. Code Health (каждую неделю):**
```bash
npm run mcp:quick-health | grep "grade:"
# Цель: >= B
```

**3. Audit Frequency (каждый месяц):**
```bash
npm run mcp:audit-summary | grep "Total Audits"
# Цель: >= 10 audits/week
```

**Dashboard (опционально):**
- См. [AUTO_INTEGRATION.md секция 7.3](AUTO_INTEGRATION.md#73-мониторинг-dashboard-опционально)

---

## 🆘 Нужна еще помощь?

- **Проблемы и ошибки:** [TROUBLESHOOTING.md](TROUBLESHOOTING.md)
- **Непонятные термины:** [GLOSSARY.md](GLOSSARY.md)
- **Примеры использования:** [USAGE_GUIDE.md](USAGE_GUIDE.md)
- **Все команды:** [CHEATSHEET.md](CHEATSHEET.md)
- **Автоматизация:** [AUTO_INTEGRATION.md](AUTO_INTEGRATION.md)

---

**Версия:** 1.0.0
**Последнее обновление:** 2025-11-10
